package net.shaidullin.code_maker.core.metadata;

import net.shaidullin.code_maker.integration.IntegrationElement;

/**
 * Leaf metadata is the element that is stored by {@link IntegrationElement}.
 * It may contains a DTO, SQL or Service definitions
 */
public interface LeafMetadata extends Metadata {
}
